<mah:MetroWindow x:Class="FEngViewer.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FEngViewer.WPF"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:system="clr-namespace:System;assembly=System.Runtime"
        xmlns:viewModels="clr-namespace:FEngViewer.WPF.ViewModels"
        xmlns:views="clr-namespace:FEngViewer.WPF.Views"
        mc:Ignorable="d"
        Title="FEngViewer" Height="720" Width="1280"
        TitleCharacterCasing="Normal"
        d:DataContext="{d:DesignInstance viewModels:MainViewModel, IsDesignTimeCreatable=True}">
    <Window.InputBindings>
        <KeyBinding Modifiers="Control" Key="O" Command="{Binding OpenCommand}"></KeyBinding>
    </Window.InputBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="File">
                <MenuItem Header="Open" Command="{Binding OpenCommand}" InputGestureText="Ctrl+O"/>
                <!-- TODO: Enable saving -->
                <MenuItem Header="Save" IsEnabled="False" />
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem Header="About"></MenuItem>
            </MenuItem>
        </Menu>
        <Border Grid.Row="1" BorderThickness="0 0 0 1" BorderBrush="{StaticResource MahApps.Brushes.Button.Border}">
            <ToolBarTray>
                <ToolBar Band="1" BandIndex="1">
                    <Button IsEnabled="False">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconForkAwesome Kind="PauseCircle" Margin="0,2,4,0" />
                            <TextBlock Text="Pause"></TextBlock>
                        </StackPanel>
                    </Button>
                    <!-- TODO: Add playback speed button -->
                </ToolBar>
            </ToolBarTray>
        </Border>
        <!--<TabControl Grid.Row="2">
            <TabControl.Template>
                <ControlTemplate TargetType="{x:Type TabControl}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                            <TabPanel x:Name="HeaderPanel" IsItemsHost="True" Margin="0,4,0,0"/>
                        </ScrollViewer>
                        <ContentPresenter x:Name="PART_SelectedContentHost" Margin="4" ContentSource="SelectedContent" Grid.Row="1"/>
                    </Grid>
                </ControlTemplate>
            </TabControl.Template>
            <TabItem Header="Welcome"></TabItem>
            <TabItem Header="Package: TestPackage.fng"></TabItem>
            <TabItem Header="Package: TestPackage2.fng"></TabItem>
            <TabItem Header="Package: TestPackage3.fng"></TabItem>
        </TabControl>-->
        <views:PackageView Grid.Row="2"></views:PackageView>
        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock>Hello</TextBlock>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</mah:MetroWindow>
